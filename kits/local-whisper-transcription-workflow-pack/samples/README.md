# samples/

Drop a short, **consent-cleared** clip here for local testing, e.g. `sample-voice-memo.m4a`.

No audio ships in the zip (keeps the pack small and avoids shipping voices).

```bash
bash scripts/transcribe.sh samples/your-clip.m4a samples/out
```
